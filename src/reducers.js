import {combineReducers} from 'redux';

export const app = (state = {}, action) => {

    switch(action.type) {
        default:
            return state;
    }
};

export default combineReducers({
    app
});


